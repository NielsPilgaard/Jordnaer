<MudImage Src="@Src" Alt="Striplet linje" Class="@Class"></MudImage>

@code {
    [Parameter]
    public string? Class { get; set; }
    [Parameter]
    public required MiniDividerColor Color { get; set; }

    private string Src => $"images/dotted-line-{Color.ToString().ToLower()}";
}
