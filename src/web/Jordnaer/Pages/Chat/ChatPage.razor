@page "/chat"
@page "/chat/{ActiveChat:guid}"

@attribute [Authorize]

<MudPaper Elevation="2">
    <ChatComponent ChatId="ActiveChat" @rendermode="InteractiveServer"/>
</MudPaper>

@code
{
    [CascadingParameter]
    private HttpContext HttpContext { get; set; } = default!;

    [Parameter]
    public Guid? ActiveChat { get; set; }
}
